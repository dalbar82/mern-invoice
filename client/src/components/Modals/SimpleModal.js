import React from 'react'

function SimpleModal({ setModal, title }) {
	return (
		<>
    <div>
      
    </div>
			<div className='simple-modal-title'>{title}</div>
		</>
	)
}

export default SimpleModal
